import 'package:flutter/material.dart';
import 'counter_view.dart';

void main() {
  // Bungkus CounterView dengan MaterialApp
  runApp(
    const MaterialApp(
      home: CounterView(), 
    ),
  );
}